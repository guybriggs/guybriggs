// modules/components/AttractionRadius.js

/**
 * Creates an AttractionRadius component.
 * @param {number} radius - The radius within which the supplier can attract demanders.
 * @returns {Object} The AttractionRadius component.
 */
export function AttractionRadiusComponent(radius) {
    return { radius };
  }
  